import Nav from "@components/Nav";
import React from "react";

export const metadata = {
  title: "Contact Us",
};

const page = () => {
  return (
    <div>
      <Nav />
    </div>
  );
};

export default page;
